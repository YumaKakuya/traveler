# PC-6 Python fixture: declaration (function_definition),
#                       statement (return_statement),
#                       expression (binary_operator, identifier, string)
def greet(name):
    return "Hello, " + name
